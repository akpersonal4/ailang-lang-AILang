# AILang

AILang is an AI-first programming language compiler project.

## Development

- Create and activate a virtual environment.
- Install development tools with `pip install -e . pytest black ruff mypy`.
- Run `pytest`, `ruff`, `black --check .`, and `mypy` to validate changes.
