# Recursion

Computes factorial using recursive function calls.

## Concepts Demonstrated

- Recursive function with base case
- Return values from recursive calls
- Mathematical recursion pattern

## How to Run

```bash
ail run main.ail
```

## What to Try

- Change the argument to `factorial()` to compute different values
- Add a recursive `power(base, exp)` function alongside `factorial`
