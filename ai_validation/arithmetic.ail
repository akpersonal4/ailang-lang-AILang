fn main() {
    let sum = 1 + 2;
    let diff = 10 - 5;
    let product = 3 * 4;
    let quotient = 10 / 3;
    let remainder = 17 % 5;
    let neg = -42;
    print(sum, diff, product, quotient, remainder, neg);
    return 0
}